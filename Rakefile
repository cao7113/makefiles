# require install ruby and rake gem
# https://github.com/ruby/rake
# https://ruby.github.io/rake/

desc "hello rake task"
task :hello do
  puts "hello rakefile"
end

desc "default task"
task :default do
  puts "this is default task"
end
